// "use client";

import { motion } from "framer-motion";
import Switcher from "./Switcher";

export default function MenuItem({}) {
  return (
    <motion.div>
      <div>
        <Switcher />
      </div>
    </motion.div>
  );
}
